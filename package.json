{
  "name": "@fugood/image-mosaic",
  "version": "1.0.1",
  "main": "lib/index.js",
  "scripts": {
    "build": "babel src --out-dir lib --ignore __tests__",
    "test": "jest",
    "prepublish": "yarn build && yarn test"
  },
  "keywords": ["image", "mosaic", "montage", "node", "canvas"],
  "license": "MIT",
  "repository": "mybigday/image-mosaic",
  "dependencies": {
    "canvas": "^2.6.0"
  },
  "devDependencies": {
    "babel-cli": "^6.26.0",
    "babel-core": "^6.26.0",
    "babel-preset-env": "^1.6.1",
    "babel-preset-stage-0": "^6.24.1",
    "jest": "^22.4.0",
    "jest-image-snapshot": "^2.3.0"
  },
  "jest": {
    "testEnvironment": "node"
  }
}
